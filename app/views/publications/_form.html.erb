<%= form_with(model: publication, class: "card p-3", :html => {:style=> "width: 500px;"}) do |form| %>
  <% if publication.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(publication.errors.count, "error") %> prohibited this publication from being saved:</h2>

      <ul>
        <% publication.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, "Título", style: "display: block" %>
    <%= form.text_field :title, class:"form-control" %>
  </div>
  <div>
    <%= form.label :description, "Descripción", style: "display: block" %>
    <%= form.text_area :description, class:"form-control" %>
  </div>
  <div>
    <%= form.submit "Crear publicación", class:"btn btn-primary my-3" %>
  </div>
<% end %>
